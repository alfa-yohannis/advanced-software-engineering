mp3_bucket     = "tts-prod"
internal_token = "changeme-production"

minio_root_user = "minioadmin"
# minio_root_password = "[REDACTED but it is the same as the username, which is the default for MinIO - change this in real production!]"

worker_replicas = 4
